runtime: php55
api_version: 1
threadsafe: true

handlers:
- url: /
  script: index.php

- url: /(.+\.php)
  script: \1

# [START env]
env_variables:
  # Replace USER, PASSWORD, DATABASE, and CONNECTION_NAME with the
  # values obtained when configuring your Cloud SQL instance.
  MYSQL_DSN: mysql:unix_socket=/cloudsql/autocomplete-185121:us-central1:autocomplete;dbname=products
  MYSQL_USER: root
  MYSQL_PASSWORD: gBp8HKuas8tc6zfb
# [END env]
